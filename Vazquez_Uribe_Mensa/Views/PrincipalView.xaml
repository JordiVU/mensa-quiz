<UserControl x:Class="Vazquez_Uribe_Mensa.Views.PrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1106">

    <UserControl.Resources>
        <FontFamily x:Key="IconFont">Segoe MDL2 Assets, Segoe UI Symbol</FontFamily>

        <!-- btn naranja -->
        <Style x:Key="OrangeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF9800"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F57C00"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- btn iconos chicos -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <TextBlock FontFamily="{StaticResource IconFont}" 
                                       Text="{TemplateBinding Content}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" 
                                       FontSize="16"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#F8F9FA" Padding="40">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- cabecera -->
            <Grid Grid.Row="0" Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mis Quizzes" FontSize="28" FontWeight="SemiBold" Foreground="Black" Margin="0,0,0,5"/>
                    <TextBlock Text="Gestiona y organiza tus colecciones de quizzes" FontSize="18" Foreground="#6B7280"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource OrangeButtonStyle}" 
                     Command="{Binding CrearQuizCommand}" VerticalAlignment="Top" Margin="0,5,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE710;" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Crear Nuevo Quiz" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- grid tarjetas -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Quizzes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" 
                        CornerRadius="8" Width="400" Margin="0,0,25,25">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Opacity="0.03" BlurRadius="10" ShadowDepth="2"/>
                                </Border.Effect>
                                <StackPanel>
                                    <StackPanel Margin="20">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold"/>
                                            <Border Grid.Column="1" Background="#FFF8E1" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                                <TextBlock Foreground="#FF9800" FontSize="12" FontWeight="SemiBold">
                                        <Run Text="{Binding Questions.Count, Mode=OneWay}"/>
                                        <Run Text=" preguntas"/>
                                                </TextBlock>
                                            </Border>
                                        </Grid>
                                        <TextBlock Text="{Binding Description}" Foreground="#6B7280" 
                                       TextWrapping="Wrap" Margin="0,15,0,0" FontSize="14"/>
                                        <StackPanel Margin="0,20,0,0">
                                            <TextBlock Foreground="#9CA3AF" FontSize="11">
                                    <Run Text="Creado: "/>
                                    <Run Text="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}, Mode=OneWay}"/>
                                            </TextBlock>
                                            <TextBlock Foreground="#9CA3AF" FontSize="11" Margin="0,2,0,0">
                                    <Run Text="Modificado: "/>
                                    <Run Text="{Binding UpdatedAt, StringFormat={}{0:dd/MM/yyyy}, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                    <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="15">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Editar -->
                                            <Button Grid.Column="0" Style="{StaticResource OrangeButtonStyle}" Padding="0,8" Margin="0,0,10,0"
                                                    Command="{Binding DataContext.EditarQuizCommand, 
                                                              RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock FontFamily="{StaticResource IconFont}" Text="&#xE70F;" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="Editar" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>

                                            <!-- Play -->
                                            <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" Content="&#xE768;" Margin="5,0"
                                                Command="{Binding DataContext.IniciarQuizCommand,
                                                          RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>

                                            <!-- Duplicar -->
                                            <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" Content="&#xE8C8;" Margin="5,0"
                                                Command="{Binding DataContext.DuplicarQuizCommand,
                                                          RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                            <!-- Borrar -->
                                            <Button Grid.Column="3" Style="{StaticResource IconButtonStyle}" Content="&#xE74D;" Margin="5,0,0,0"
                                                Command="{Binding DataContext.BorrarQuizCommand,
                                                          RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>